pipeline {
    agent any
     stages {
        stage('Example') {
            steps {
		
                 echo "Hello- World "
                sh 'lsblk'
               sh 'date'
               sh 'sudo la -l /etc/hosts'
               sh 'ls -alh'
             }
          }
      }
}
